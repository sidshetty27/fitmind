# FastAPI core (Phase 1). AI/Stripe deps are added in later phases.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Database (Phase 3)
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
# psycopg 3, binary wheel: no local libpq/build toolchain required, and one
# driver serves both the async app engine and the sync Alembic engine.
psycopg[binary]==3.2.3

# Auth (Phase 4)
# PyJWT verifies Clerk's RS256 session tokens against their JWKS; the [crypto]
# extra pulls in `cryptography` for RSA signature checking. Svix webhook
# signatures are verified with the standard library (see app/core/svix.py), so no
# svix package is needed.
pyjwt[crypto]==2.10.1
# HTTP client — used at runtime for the Clerk Backend API email fallback, and by
# the test suite's in-process ASGI client.
httpx==0.28.1

# Test tooling
pytest==8.3.4
pytest-asyncio==0.25.0
